name: Build and Deploy GitHub Pages

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Check out the repository so the workflow can access its contents
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Set up Ruby (using a Ruby version that works for you)
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'           # Adjust Ruby version as needed
          bundler-cache: true

      # Install dependencies from your Gemfile, which is located in the Jekyll source folder
      - name: Install dependencies
        working-directory: ./page/just-the-docs-main
        run: bundle install

      # Build the Jekyll site:
      # The working directory is set to the Jekyll source folder, and the output is sent to ../../docs.
      - name: Build site with Jekyll
        working-directory: ./page/just-the-docs-main
        run: bundle exec jekyll build --destination ../../docs

      # Deploy the built site to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
